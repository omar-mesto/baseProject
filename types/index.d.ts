declare global {
      interface LoginForm{
        email:string,
        password:string
      }  
      type ToasterColors = {
        sucess:"success",
        error:"error"
      } 
  }
  
  export { };

